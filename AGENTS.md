# 项目协作与提交规则

- 前端代码目录为 `D:\Quill`；规格唯一维护源为 `D:\agent\SPEC\Quill\SPEC.md`，规格目录的 `AGENTS.md` 同样适用。
- 每次修改先更新规格中的行为、约束和验收标准，再修改代码；缺陷修复也先明确预期行为与回归验收要求。
- 仓库中的 `docs/SPEC.md` 仅为规格同步快照，不独立编辑；提交前从唯一维护源同步，与相关代码一同提交。
- 用户已指定 GitHub 账号 `nuonuoluoya`、仓库 `quill`（`https://github.com/nuonuoluoya/quill.git`）。每次修改完成并通过相关检查后，提交本次相关变更并推送到该仓库的 `origin`，无需重复询问常规提交和推送授权；检查或推送失败时如实报告。
- 保留工作区无关的已有修改，只暂存与当前任务相关的文件或差异。

- 用户授权提交时，先检查当前分支、远端和工作区，再提交与任务相关的变更。
- 每次提交若包含特性、用户可见行为、配置方式、启动方式或接口使用变化，必须在同一次提交中更新 README.md，说明最终行为及使用方法。仅内部重构且使用方式不变时不必添加无关的 README 修改。
- 提交前运行与改动相关的检查。原生页面或组件改动需要检查模板、组件依赖和相关交互。
- project.config.json、project.private.config.json、config/config.js、.env 文件和 .codex-tools/ 只保留在本地，不提交。仓库保留无个人信息的示例配置。
- 不在小程序代码中保存 AppSecret、访问令牌或服务端密钥。新增配置需要同步示例与初始化说明。
- 不覆盖用户本地配置，不强制推送，不重写远端历史，除非用户明确要求。
